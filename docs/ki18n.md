# `ki18n` In `ktools-python`

`ktools-python/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` is not yet implemented in the current Python workspace
- when implementation work starts, this page should be replaced by real docs in
  the workspace
