# Bootstrap Demo

Minimal import and parse smoke test for the Python `kcli` package.

This demo shows:

- bootstrap-local import bootstrapping into the component `src/` tree
- a top-level alias `-v -> --verbose`
- one top-level flag handler
- a successful `parse_or_exit()` flow followed by a confirmation message
