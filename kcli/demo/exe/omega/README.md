# Omega Demo

Full-featured CLI integration showcase for Python `kcli` plus the demo SDKs.

This demo exercises:

- combined alpha, beta, gamma, and local `--build-*` inline roots
- alias coverage for top-level and inline options
- positional-handler registration
- inline root override from `--gamma` to `--newgamma`
