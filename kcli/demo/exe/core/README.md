# Core Demo

Basic integration showcase for Python `kcli` plus the alpha demo SDK.

This demo shows:

- local top-level handlers such as `--verbose` and `--output`
- alias registration for `-v`, `-out`, and `-a`
- imported inline parsing through the alpha SDK
- post-parse usage output after a successful validation pass
