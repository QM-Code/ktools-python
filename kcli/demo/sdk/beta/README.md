# Beta Demo SDK

Reusable inline parser for the `--beta` root.

This module demonstrates:

- required-value inline handlers for `--beta-profile` and `--beta-workers`
- handler-local validation that converts Python exceptions into CLI failures
- a demo of application-level validation layered on top of `kcli`
