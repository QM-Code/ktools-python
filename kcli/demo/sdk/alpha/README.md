# Alpha Demo SDK

Minimal reusable inline parser for the `--alpha` root.

This module demonstrates:

- a required-value inline handler at `--alpha-message`
- an optional-value inline handler at `--alpha-enable`
- handler output that reflects `HandlerContext.option` and collected values
