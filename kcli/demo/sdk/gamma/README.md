# Gamma Demo SDK

Reusable inline parser for the `--gamma` root.

This module demonstrates:

- an optional-value handler at `--gamma-strict`
- a required-value handler at `--gamma-tag`
- a root that can be renamed by the consuming executable
