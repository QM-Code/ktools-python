# Omega Demo

Full-featured trace showcase for Python `ktrace` plus the demo SDKs.

This demo exercises:

- local executable channels plus imported alpha, beta, and gamma trace sources
- selector matching across multiple namespaces
- `--trace-files`, `--trace-functions`, and `--trace-timestamps`
- wildcard and brace-selector coverage across nested channels
