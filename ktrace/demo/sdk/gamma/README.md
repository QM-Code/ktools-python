# Gamma Demo SDK

Reusable library-facing trace source for the `gamma` namespace.

This module demonstrates:

- a compact trace source with `physics` and `metrics` channels
- SDK-defined channels imported into an executable logger
- selector coverage across multiple namespaces
