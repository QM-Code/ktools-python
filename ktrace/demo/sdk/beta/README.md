# Beta Demo SDK

Reusable library-facing trace source for the `beta` namespace.

This module demonstrates:

- a compact trace source with `io` and `scheduler` channels
- explicit color assignment
- executable-side import of SDK-owned channels
