# Alpha Demo SDK

Reusable library-facing trace source for the `alpha` namespace.

This module demonstrates:

- a shared `TraceLogger("alpha")`
- nested channels such as `net.gamma.deep`
- mixed default and explicit colors
- a helper that emits across the declared channels for selector testing
